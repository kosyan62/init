echo "Имя удаляемого пользователя?"
read name

exist=`cat /etc/passwd | grep $name`
if [ "$exist" != "$NULL" ]; then
	sudo passwd -l $name
	if [ "$(pgrep -u $name)" != "$NULL" ]; then
		sudo kill -9 $(pgrep -u $name)
	fi
	sudo userdel -r $name
else
	echo "Пользователь не существует"
fi
